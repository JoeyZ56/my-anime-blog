import styles from "./account.module.scss";

const Account = ({ getUser }) => {
  return (
    <div className="account-container">{/* <h1>{getUser.name}</h1> */}</div>
  );
};

export default Account;
